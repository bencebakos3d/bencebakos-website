<section class="section-landing">
    <div class="landing-video-wrapper">
        <img src="./engine_video/engine_1.jpg" id="landing-video" alt="">
    </div>
</section>

<style>
    .section-landing {
        width: 100vw;
        height: 1500px;
    }
    .landing-video-wrapper {
        position: sticky;
        width: 100%;
        height: 100vh;
        top: 0;
    }
    #landing-video {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

</style>

<script>
    const landingVideo = document.getElementById("landing-video")
    const step = 10;

function trackScrollPosition() {
  const y = window.scrollY;
  const label = Math.min(Math.floor(y/step) + 1, 100);
  const currentImage = `./engine_video/engine_${label}.jpg`;
  // Change the background image
  landingVideo.setAttribute("src", currentImage)
}

document.addEventListener("scroll", () => {
    trackScrollPosition();
}) 
</script>